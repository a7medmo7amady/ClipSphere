## Configs

---

```
  - Client → http://localhost:3000                                                                                    
  - Server → http://localhost:5000
  - MinIO console → http://localhost:9001 (minioadmin / minioadmin123)
```

### Tests 

# for now test inlcudes Yt vids downloader 

to use it copy url 
```bash
cd Tests
python3 Script.py
```
and Past the url in the GUI + Choose vid downloading directory

